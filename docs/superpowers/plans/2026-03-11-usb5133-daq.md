# NI USB-5133 DAQ System Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** NI USB-5133 디지타이저를 활용한 Python 기반 데이터 수집 시스템 — 실시간 파형 모니터링, FFT 분석, CSV 저장 기능을 갖춘 PyQt5 GUI 앱.

**Architecture:** 계층형 모듈 구조 (device / acquisition / analysis / storage / ui). QThread 기반 백그라운드 수집 루프가 Qt Signal로 UI에 데이터를 전달. 하드웨어 없는 환경에서는 MockScope로 대체.

**Tech Stack:** Python 3.x, niscope, PyQt5, pyqtgraph, numpy, pytest

---

## File Map

| 파일 | 역할 |
|------|------|
| `usb5133_daq/device/scope.py` | NI5133 + MockScope 클래스 |
| `usb5133_daq/acquisition/worker.py` | QThread 수집 루프 |
| `usb5133_daq/analysis/fft.py` | FFT 계산 |
| `usb5133_daq/storage/csv_writer.py` | CSV 저장 |
| `usb5133_daq/ui/waveform_plot.py` | pyqtgraph 실시간 파형 위젯 |
| `usb5133_daq/ui/fft_plot.py` | pyqtgraph FFT 스펙트럼 위젯 |
| `usb5133_daq/ui/main_window.py` | 메인 PyQt 윈도우 |
| `main.py` | 앱 진입점 |
| `requirements.txt` | 의존성 목록 |
| `tests/test_scope.py` | device 레이어 유닛 테스트 |
| `tests/test_fft.py` | FFT 유닛 테스트 |
| `tests/test_csv_writer.py` | CSV 저장 유닛 테스트 |
| `tests/test_worker.py` | acquisition worker 유닛 테스트 |

---

## Chunk 1: 프로젝트 스캐폴딩 + Device 레이어

### Task 1: 프로젝트 구조 생성 및 의존성 설정

**Files:**
- Create: `requirements.txt`
- Create: `usb5133_daq/__init__.py`
- Create: `usb5133_daq/device/__init__.py`
- Create: `usb5133_daq/acquisition/__init__.py`
- Create: `usb5133_daq/analysis/__init__.py`
- Create: `usb5133_daq/storage/__init__.py`
- Create: `usb5133_daq/ui/__init__.py`
- Create: `tests/__init__.py`

- [ ] **Step 1: requirements.txt 작성**

```
niscope>=1.4.0
PyQt5>=5.15.0
pyqtgraph>=0.13.0
numpy>=1.24.0
pytest>=7.0.0
```

파일 위치: `requirements.txt` (프로젝트 루트)

- [ ] **Step 2: 패키지 디렉토리 및 빈 `__init__.py` 생성**

```bash
mkdir -p usb5133_daq/device usb5133_daq/acquisition usb5133_daq/analysis usb5133_daq/storage usb5133_daq/ui tests
touch usb5133_daq/__init__.py usb5133_daq/device/__init__.py usb5133_daq/acquisition/__init__.py usb5133_daq/analysis/__init__.py usb5133_daq/storage/__init__.py usb5133_daq/ui/__init__.py tests/__init__.py
```

- [ ] **Step 3: 의존성 설치 확인**

```bash
pip install PyQt5 pyqtgraph numpy pytest
# niscope는 NI 드라이버 없는 환경에서 pip install이 실패할 수 있음
# CI/개발 환경에서는 아래 명령으로 선택적 설치
pip install niscope --no-deps || echo "niscope 설치 건너뜀 (MockScope로 개발 가능)"
```

Expected: PyQt5, pyqtgraph, numpy, pytest 설치 완료. niscope 설치 실패 시 무시하고 진행.

- [ ] **Step 4: Commit**

```bash
git init
git add requirements.txt usb5133_daq/ tests/
git commit -m "chore: scaffold project structure"
```

---

### Task 2: MockScope 구현 (TDD)

**Files:**
- Create: `usb5133_daq/device/scope.py`
- Test: `tests/test_scope.py`

MockScope를 먼저 구현한다. NI5133은 실제 드라이버가 필요하므로 인터페이스만 정의하고, 테스트는 MockScope로 검증한다.

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_scope.py
import numpy as np
import pytest
from usb5133_daq.device.scope import MockScope

VALID_VOLTAGE_RANGES = [0.5, 1.0, 2.0, 5.0]

class TestMockScope:
    def test_connect_disconnect(self):
        scope = MockScope()
        scope.connect()
        scope.disconnect()  # 예외 없이 실행돼야 함

    def test_context_manager(self):
        with MockScope() as scope:
            pass  # 예외 없이 실행돼야 함

    def test_configure_sets_params(self):
        scope = MockScope()
        scope.connect()
        scope.configure(
            sample_rate=1_000_000.0,
            record_length=1000,
            channels=[0],
            voltage_range=1.0,
        )
        assert scope.sample_rate == 1_000_000.0
        assert scope.record_length == 1000
        assert scope.channels == [0]
        assert scope.voltage_range == 1.0

    def test_configure_raises_if_not_connected(self):
        scope = MockScope()
        with pytest.raises(RuntimeError, match="connect"):
            scope.configure(1_000_000.0, 1000, [0], 1.0)

    def test_configure_raises_on_invalid_sample_rate(self):
        scope = MockScope()
        scope.connect()
        with pytest.raises(ValueError, match="sample_rate"):
            scope.configure(200_000_000.0, 1000, [0], 1.0)

    def test_configure_raises_on_invalid_voltage_range(self):
        scope = MockScope()
        scope.connect()
        with pytest.raises(ValueError, match="voltage_range"):
            scope.configure(1_000_000.0, 1000, [0], 3.0)

    def test_fetch_returns_correct_shape_one_channel(self):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)
        data = scope.fetch()
        assert data.shape == (1, 1000)

    def test_fetch_returns_correct_shape_two_channels(self):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0, 1], 1.0)
        data = scope.fetch()
        assert data.shape == (2, 1000)

    def test_fetch_returns_sine_wave(self):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)
        data = scope.fetch()
        # 사인파: 값이 [-1, 1] 범위 안에 있어야 함
        assert data.min() >= -1.1
        assert data.max() <= 1.1
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_scope.py -v
```

Expected: `ImportError` 또는 `ModuleNotFoundError`

- [ ] **Step 3: scope.py 구현**

```python
# usb5133_daq/device/scope.py
from __future__ import annotations

import numpy as np

VALID_VOLTAGE_RANGES = {0.5, 1.0, 2.0, 5.0}
MAX_SAMPLE_RATE = 100_000_000.0


class _ScopeBase:
    """NI5133과 MockScope가 공유하는 인터페이스."""

    def __enter__(self):
        self.connect()
        return self

    def __exit__(self, *args):
        self.disconnect()

    def connect(self):
        raise NotImplementedError

    def disconnect(self):
        raise NotImplementedError

    def configure(
        self,
        sample_rate: float,
        record_length: int,
        channels: list[int],
        voltage_range: float,
    ):
        raise NotImplementedError

    def start_acquisition(self) -> None:
        """수집 시작 전 호출. NI5133에서는 initiate() 실행."""

    def stop_acquisition(self) -> None:
        """수집 종료 후 호출. NI5133에서는 abort() 실행."""

    def fetch(self) -> np.ndarray:
        raise NotImplementedError


class MockScope(_ScopeBase):
    """하드웨어 없는 환경에서 테스트용 사인파 생성."""

    def __init__(self):
        self._connected = False
        self.sample_rate: float = 0.0
        self.record_length: int = 0
        self.channels: list[int] = []
        self.voltage_range: float = 0.0

    def connect(self):
        self._connected = True

    def disconnect(self):
        self._connected = False

    def configure(
        self,
        sample_rate: float,
        record_length: int,
        channels: list[int],
        voltage_range: float,
    ):
        if not self._connected:
            raise RuntimeError("connect() must be called before configure()")
        if sample_rate > MAX_SAMPLE_RATE:
            raise ValueError(
                f"sample_rate {sample_rate} exceeds maximum {MAX_SAMPLE_RATE} Hz"
            )
        if voltage_range not in VALID_VOLTAGE_RANGES:
            raise ValueError(
                f"voltage_range {voltage_range} is invalid. "
                f"Valid values: {sorted(VALID_VOLTAGE_RANGES)}"
            )
        self.sample_rate = sample_rate
        self.record_length = record_length
        self.channels = list(channels)
        self.voltage_range = voltage_range

    def start_acquisition(self) -> None:
        """수집 시작 (MockScope에서는 no-op)."""

    def stop_acquisition(self) -> None:
        """수집 종료 (MockScope에서는 no-op)."""

    def fetch(self) -> np.ndarray:
        """shape (num_channels, record_length)의 사인파 반환."""
        t = np.arange(self.record_length) / self.sample_rate
        freq = 1000.0  # 1 kHz 테스트 신호
        sine = np.sin(2 * np.pi * freq * t)
        return np.stack([sine] * len(self.channels))


class NI5133(_ScopeBase):
    """NI USB-5133 디지타이저 래퍼. niscope 드라이버 필요."""

    def __init__(self, resource_name: str = "USB0::0x3923::0x7527::0::RAW"):
        self._resource_name = resource_name
        self._session = None
        self.sample_rate: float = 0.0
        self.record_length: int = 0
        self.channels: list[int] = []
        self.voltage_range: float = 0.0

    def connect(self):
        import niscope  # noqa: PLC0415
        self._session = niscope.Session(self._resource_name)

    def disconnect(self):
        if self._session is not None:
            self._session.close()
            self._session = None

    def configure(
        self,
        sample_rate: float,
        record_length: int,
        channels: list[int],
        voltage_range: float,
    ):
        if self._session is None:
            raise RuntimeError("connect() must be called before configure()")
        if sample_rate > MAX_SAMPLE_RATE:
            raise ValueError(
                f"sample_rate {sample_rate} exceeds maximum {MAX_SAMPLE_RATE} Hz"
            )
        if voltage_range not in VALID_VOLTAGE_RANGES:
            raise ValueError(
                f"voltage_range {voltage_range} is invalid. "
                f"Valid values: {sorted(VALID_VOLTAGE_RANGES)}"
            )
        self.sample_rate = sample_rate
        self.record_length = record_length
        self.channels = list(channels)
        self.voltage_range = voltage_range

        import niscope  # noqa: PLC0415
        ch_str = ",".join(str(c) for c in channels)
        self._session.configure_vertical(
            range=voltage_range,
            coupling=niscope.VerticalCoupling.dc,  # lowercase: niscope Python API 규칙
            channel_name=ch_str,
        )
        self._session.configure_horizontal_timing(
            min_sample_rate=sample_rate,
            min_num_pts=record_length,
            ref_position=50.0,
            num_records=1,
            enforce_realtime=True,
        )

    def start_acquisition(self) -> None:
        """연속 수집 시작. AcquisitionWorker.run() 진입 시 1회 호출."""
        self._session.initiate()

    def stop_acquisition(self) -> None:
        """연속 수집 중단. AcquisitionWorker.stop() 후 호출."""
        self._session.abort()

    def fetch(self) -> np.ndarray:
        """shape (num_channels, record_length)의 파형 반환.
        start_acquisition() 이후 반복 호출 가능.
        """
        ch_str = ",".join(str(c) for c in self.channels)
        waveforms = self._session.fetch(
            channel_name=ch_str,
            num_samples=self.record_length,
        )
        return np.stack([np.array(w.samples) for w in waveforms])
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_scope.py -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: Commit**

```bash
git add usb5133_daq/device/scope.py tests/test_scope.py
git commit -m "feat: add NI5133 and MockScope device layer"
```

---

## Chunk 2: Analysis + Storage 레이어

### Task 3: FFT 분석 구현 (TDD)

**Files:**
- Create: `usb5133_daq/analysis/fft.py`
- Test: `tests/test_fft.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_fft.py
import numpy as np
import pytest
from usb5133_daq.analysis.fft import compute_fft


class TestComputeFFT:
    def test_returns_two_arrays(self):
        waveform = np.zeros(1000)
        freqs, mags = compute_fft(waveform, sample_rate=1_000_000.0)
        assert freqs is not None
        assert mags is not None

    def test_output_length_is_half_input(self):
        """단측 스펙트럼: 출력 길이 = N//2"""
        N = 1000
        waveform = np.zeros(N)
        freqs, mags = compute_fft(waveform, sample_rate=1_000_000.0)
        assert len(freqs) == N // 2
        assert len(mags) == N // 2

    def test_detects_known_frequency(self):
        """1 kHz 사인파 입력 시 1 kHz 부근에서 최대값 검출."""
        sample_rate = 100_000.0
        N = 4096
        freq_hz = 1000.0
        t = np.arange(N) / sample_rate
        waveform = np.sin(2 * np.pi * freq_hz * t)

        freqs, mags = compute_fft(waveform, sample_rate=sample_rate)
        peak_freq = freqs[np.argmax(mags)]
        assert abs(peak_freq - freq_hz) < 50.0  # 50 Hz 이내

    def test_dc_signal_peak_at_zero(self):
        """DC 신호: 0 Hz에서 최대값."""
        waveform = np.ones(1000)
        freqs, mags = compute_fft(waveform, sample_rate=1_000_000.0)
        assert freqs[np.argmax(mags)] == pytest.approx(0.0, abs=1.0)

    def test_freqs_are_non_negative(self):
        waveform = np.random.randn(512)
        freqs, mags = compute_fft(waveform, sample_rate=50_000.0)
        assert np.all(freqs >= 0)

    def test_mags_are_non_negative(self):
        waveform = np.random.randn(512)
        freqs, mags = compute_fft(waveform, sample_rate=50_000.0)
        assert np.all(mags >= 0)
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_fft.py -v
```

Expected: `ImportError`

- [ ] **Step 3: fft.py 구현**

```python
# usb5133_daq/analysis/fft.py
from __future__ import annotations

import numpy as np


def compute_fft(
    waveform: np.ndarray, sample_rate: float
) -> tuple[np.ndarray, np.ndarray]:
    """단측 FFT 스펙트럼 계산.

    Args:
        waveform: 1D numpy 배열 (단일 채널 파형)
        sample_rate: 샘플레이트 (Hz)

    Returns:
        (freqs, magnitudes): 주파수 배열(Hz), 크기 배열 (둘 다 길이 N//2)
    """
    N = len(waveform)
    window = np.hanning(N)
    windowed = waveform * window

    fft_vals = np.fft.fft(windowed)
    # 단측 스펙트럼: 첫 N//2 포인트만 사용
    half = N // 2
    magnitudes = np.abs(fft_vals[:half]) * 2.0 / N
    freqs = np.fft.fftfreq(N, d=1.0 / sample_rate)[:half]

    return freqs, magnitudes
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_fft.py -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: Commit**

```bash
git add usb5133_daq/analysis/fft.py tests/test_fft.py
git commit -m "feat: add FFT analysis module"
```

---

### Task 4: CSV 저장 구현 (TDD)

**Files:**
- Create: `usb5133_daq/storage/csv_writer.py`
- Test: `tests/test_csv_writer.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_csv_writer.py
import csv
import tempfile
from pathlib import Path

import numpy as np
import pytest
from usb5133_daq.storage.csv_writer import save_waveform


class TestSaveWaveform:
    def test_creates_file(self, tmp_path):
        waveform = np.zeros((1, 100))
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=1_000_000.0, filename=out)
        assert Path(out).exists()

    def test_single_channel_columns(self, tmp_path):
        """1채널: 헤더가 time, ch0"""
        waveform = np.zeros((1, 10))
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=10.0, filename=out)
        with open(out) as f:
            reader = csv.reader(f)
            header = next(reader)
        assert header == ["time", "ch0"]

    def test_two_channel_columns(self, tmp_path):
        """2채널: 헤더가 time, ch0, ch1"""
        waveform = np.zeros((2, 10))
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=10.0, filename=out)
        with open(out) as f:
            reader = csv.reader(f)
            header = next(reader)
        assert header == ["time", "ch0", "ch1"]

    def test_time_axis_correct(self, tmp_path):
        """time 컬럼: 0부터 (N-1)/sample_rate까지"""
        N = 5
        sr = 10.0
        waveform = np.zeros((1, N))
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=sr, filename=out)

        with open(out) as f:
            reader = csv.reader(f)
            next(reader)  # 헤더 스킵
            rows = list(reader)

        times = [float(r[0]) for r in rows]
        expected = [i / sr for i in range(N)]
        assert len(times) == N
        for t, e in zip(times, expected):
            assert abs(t - e) < 1e-9

    def test_data_values_preserved(self, tmp_path):
        waveform = np.array([[1.0, 2.0, 3.0]])
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=1.0, filename=out)

        with open(out) as f:
            reader = csv.reader(f)
            next(reader)
            rows = list(reader)

        values = [float(r[1]) for r in rows]
        assert values == pytest.approx([1.0, 2.0, 3.0])

    def test_row_count_matches_record_length(self, tmp_path):
        N = 200
        waveform = np.zeros((2, N))
        out = str(tmp_path / "out.csv")
        save_waveform(waveform, sample_rate=1_000.0, filename=out)

        with open(out) as f:
            reader = csv.reader(f)
            next(reader)
            rows = list(reader)
        assert len(rows) == N
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
pytest tests/test_csv_writer.py -v
```

Expected: `ImportError`

- [ ] **Step 3: csv_writer.py 구현**

```python
# usb5133_daq/storage/csv_writer.py
from __future__ import annotations

import numpy as np


def save_waveform(
    waveform: np.ndarray,
    sample_rate: float,
    filename: str,
) -> None:
    """파형 데이터를 CSV로 저장.

    Args:
        waveform: shape (num_channels, record_length)
        sample_rate: 샘플레이트 (Hz) — 시간축 생성에 사용
        filename: 저장할 파일 경로
    """
    num_channels, record_length = waveform.shape
    time_axis = np.arange(record_length) / sample_rate

    # shape: (record_length, 1 + num_channels)
    data = np.column_stack([time_axis] + [waveform[i] for i in range(num_channels)])
    header = "time," + ",".join(f"ch{i}" for i in range(num_channels))

    # comments="" → 헤더 줄에 '#' 접두사 없이 저장
    np.savetxt(filename, data, delimiter=",", header=header, comments="")
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pytest tests/test_csv_writer.py -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 5: Commit**

```bash
git add usb5133_daq/storage/csv_writer.py tests/test_csv_writer.py
git commit -m "feat: add CSV storage module"
```

---

## Chunk 3: Acquisition Worker

### Task 5: AcquisitionWorker 구현 (TDD)

**Files:**
- Create: `usb5133_daq/acquisition/worker.py`
- Test: `tests/test_worker.py`

PyQt5 QThread를 테스트할 때는 `QApplication` 인스턴스가 필요하다. `pytest-qt`는 범위 제외이므로, `conftest.py`에서 수동으로 `QApplication`을 생성한다.

- [ ] **Step 1: conftest.py 작성**

```python
# tests/conftest.py
import sys
import pytest
from PyQt5.QtWidgets import QApplication


@pytest.fixture(scope="session")
def qapp():
    app = QApplication.instance() or QApplication(sys.argv)
    yield app
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_worker.py
import time
import numpy as np
import pytest
from usb5133_daq.acquisition.worker import AcquisitionWorker
from usb5133_daq.device.scope import MockScope


class TestAcquisitionWorker:
    def test_emits_data_ready(self, qapp):
        """수집 시작 시 data_ready 시그널이 발생해야 함."""
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)

        worker = AcquisitionWorker(scope)
        received = []
        worker.data_ready.connect(lambda d: received.append(d))

        worker.start()
        # msleep(10) 포함 → 약 50ms면 최소 3회 이상 발생
        time.sleep(0.1)
        worker.stop()
        worker.wait(2000)  # 최대 2초 대기

        assert len(received) > 0

    def test_data_shape_one_channel(self, qapp):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 500, [0], 1.0)

        worker = AcquisitionWorker(scope)
        received = []
        worker.data_ready.connect(lambda d: received.append(d))

        worker.start()
        time.sleep(0.1)
        worker.stop()
        worker.wait(2000)

        assert received[0].shape == (1, 500)

    def test_data_shape_two_channels(self, qapp):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 500, [0, 1], 1.0)

        worker = AcquisitionWorker(scope)
        received = []
        worker.data_ready.connect(lambda d: received.append(d))

        worker.start()
        time.sleep(0.1)
        worker.stop()
        worker.wait(2000)

        assert received[0].shape == (2, 500)

    def test_stop_terminates_thread(self, qapp):
        scope = MockScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)

        worker = AcquisitionWorker(scope)
        worker.start()
        time.sleep(0.05)
        worker.stop()
        finished = worker.wait(2000)

        assert finished  # 2초 이내에 종료돼야 함

    def test_error_signal_on_fetch_failure(self, qapp):
        """fetch()가 예외를 던지면 error_occurred 시그널 발생."""
        class FailingScope(MockScope):
            def fetch(self):
                raise RuntimeError("simulated fetch error")

        scope = FailingScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)

        worker = AcquisitionWorker(scope)
        errors = []
        worker.error_occurred.connect(lambda e: errors.append(e))

        worker.start()
        time.sleep(0.1)
        worker.stop()
        worker.wait(2000)

        assert len(errors) > 0
        assert "simulated fetch error" in errors[0]

    def test_start_acquisition_failure_emits_error(self, qapp):
        """start_acquisition()이 실패하면 error_occurred 시그널 발생, 조용한 종료 없음."""
        class BadStartScope(MockScope):
            def start_acquisition(self):
                raise RuntimeError("start failed")

        scope = BadStartScope()
        scope.connect()
        scope.configure(1_000_000.0, 1000, [0], 1.0)

        worker = AcquisitionWorker(scope)
        errors = []
        worker.error_occurred.connect(lambda e: errors.append(e))

        worker.start()
        time.sleep(0.05)
        worker.wait(2000)

        assert len(errors) == 1
        assert "start failed" in errors[0]
```

- [ ] **Step 3: 테스트 실패 확인**

```bash
pytest tests/test_worker.py -v
```

Expected: `ImportError`

- [ ] **Step 4: worker.py 구현**

```python
# usb5133_daq/acquisition/worker.py
from __future__ import annotations

import threading

import numpy as np
from PyQt5.QtCore import QThread, pyqtSignal


class AcquisitionWorker(QThread):
    """백그라운드 수집 루프.

    Signals:
        data_ready: shape (num_channels, record_length)의 np.ndarray 전달
        error_occurred: 오류 메시지 문자열 전달
    """

    data_ready = pyqtSignal(object)
    error_occurred = pyqtSignal(str)

    def __init__(self, scope, parent=None):
        super().__init__(parent)
        self._scope = scope
        self._stop_event = threading.Event()

    def run(self):
        self._stop_event.clear()
        try:
            self._scope.start_acquisition()
        except Exception as exc:
            self.error_occurred.emit(str(exc))
            return  # start 실패 시 UI에 알리고 조용히 종료
        try:
            while not self._stop_event.is_set():
                try:
                    data = self._scope.fetch()
                    self.data_ready.emit(data)
                    # NI5133 연속 수집 속도 제어: UI 과부하 방지
                    # MockScope는 즉각 반환하므로 sleep 없으면 CPU 점유 100%
                    self.msleep(10)  # QThread 내장 10ms 슬립
                except Exception as exc:
                    self.error_occurred.emit(str(exc))
                    break  # 오류 발생 시 루프 종료
        finally:
            self._scope.stop_acquisition()

    def stop(self):
        """스레드 안전하게 수집 루프 종료."""
        self._stop_event.set()
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
pytest tests/test_worker.py -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 6: 전체 테스트 재실행**

```bash
pytest tests/ -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 7: Commit**

```bash
git add usb5133_daq/acquisition/worker.py tests/test_worker.py tests/conftest.py
git commit -m "feat: add QThread-based acquisition worker"
```

---

## Chunk 4: UI 레이어 + 앱 진입점

GUI 컴포넌트는 수동 테스트이므로 테스트 파일 없음.

### Task 6: WaveformPlot 위젯

**Files:**
- Create: `usb5133_daq/ui/waveform_plot.py`

- [ ] **Step 1: waveform_plot.py 구현**

```python
# usb5133_daq/ui/waveform_plot.py
from __future__ import annotations

import numpy as np
import pyqtgraph as pg
from PyQt5.QtWidgets import QWidget, QVBoxLayout


CHANNEL_COLORS = ["#00BFFF", "#FF6B6B"]  # CH0: 파랑, CH1: 빨강


class WaveformPlot(QWidget):
    """실시간 파형 플롯 위젯.

    update_waveform(waveform, sample_rate) 호출로 데이터 갱신.
    waveform shape: (num_channels, record_length)
    """

    def __init__(self, parent=None):
        super().__init__(parent)
        self._plot_widget = pg.PlotWidget(title="파형")
        self._plot_widget.setLabel("left", "전압 (V)")
        self._plot_widget.setLabel("bottom", "시간 (s)")
        self._plot_widget.addLegend()
        self._plot_widget.showGrid(x=True, y=True, alpha=0.3)

        self._curves: list[pg.PlotDataItem] = []

        layout = QVBoxLayout(self)
        layout.setContentsMargins(0, 0, 0, 0)
        layout.addWidget(self._plot_widget)

    def update_waveform(self, waveform: np.ndarray, sample_rate: float) -> None:
        num_channels, record_length = waveform.shape
        t = np.arange(record_length) / sample_rate

        # 채널 수가 바뀌면 커브 재생성
        if len(self._curves) != num_channels:
            for c in self._curves:
                self._plot_widget.removeItem(c)
            self._curves = [
                self._plot_widget.plot(
                    pen=pg.mkPen(CHANNEL_COLORS[i % len(CHANNEL_COLORS)], width=1),
                    name=f"CH{i}",
                )
                for i in range(num_channels)
            ]

        for i, curve in enumerate(self._curves):
            curve.setData(t, waveform[i])
```

- [ ] **Step 2: Commit**

```bash
git add usb5133_daq/ui/waveform_plot.py
git commit -m "feat: add WaveformPlot widget"
```

---

### Task 7: FFTPlot 위젯

**Files:**
- Create: `usb5133_daq/ui/fft_plot.py`

- [ ] **Step 1: fft_plot.py 구현**

```python
# usb5133_daq/ui/fft_plot.py
from __future__ import annotations

import numpy as np
import pyqtgraph as pg
from PyQt5.QtWidgets import QWidget, QVBoxLayout

from usb5133_daq.analysis.fft import compute_fft

CHANNEL_COLORS = ["#00BFFF", "#FF6B6B"]


class FFTPlot(QWidget):
    """FFT 스펙트럼 플롯 위젯.

    update_fft(waveform, sample_rate) 호출로 데이터 갱신.
    waveform shape: (num_channels, record_length)
    """

    def __init__(self, parent=None):
        super().__init__(parent)
        self._plot_widget = pg.PlotWidget(title="FFT 스펙트럼")
        self._plot_widget.setLabel("left", "크기")
        self._plot_widget.setLabel("bottom", "주파수 (Hz)")
        self._plot_widget.addLegend()
        self._plot_widget.showGrid(x=True, y=True, alpha=0.3)

        self._curves: list[pg.PlotDataItem] = []

        layout = QVBoxLayout(self)
        layout.setContentsMargins(0, 0, 0, 0)
        layout.addWidget(self._plot_widget)

    def update_fft(self, waveform: np.ndarray, sample_rate: float) -> None:
        num_channels = waveform.shape[0]

        if len(self._curves) != num_channels:
            for c in self._curves:
                self._plot_widget.removeItem(c)
            self._curves = [
                self._plot_widget.plot(
                    pen=pg.mkPen(CHANNEL_COLORS[i % len(CHANNEL_COLORS)], width=1),
                    name=f"CH{i}",
                )
                for i in range(num_channels)
            ]

        for i, curve in enumerate(self._curves):
            freqs, mags = compute_fft(waveform[i], sample_rate)
            curve.setData(freqs, mags)
```

- [ ] **Step 2: Commit**

```bash
git add usb5133_daq/ui/fft_plot.py
git commit -m "feat: add FFTPlot widget"
```

---

### Task 8: MainWindow 구현

**Files:**
- Create: `usb5133_daq/ui/main_window.py`

- [ ] **Step 1: main_window.py 구현**

```python
# usb5133_daq/ui/main_window.py
from __future__ import annotations

import numpy as np
from PyQt5.QtCore import Qt
from PyQt5.QtWidgets import (
    QCheckBox,
    QComboBox,
    QFileDialog,
    QGroupBox,
    QHBoxLayout,
    QLabel,
    QLineEdit,
    QMainWindow,
    QMessageBox,
    QPushButton,
    QSplitter,
    QStatusBar,
    QVBoxLayout,
    QWidget,
)

from usb5133_daq.acquisition.worker import AcquisitionWorker
from usb5133_daq.device.scope import NI5133, MockScope
from usb5133_daq.storage.csv_writer import save_waveform
from usb5133_daq.ui.fft_plot import FFTPlot
from usb5133_daq.ui.waveform_plot import WaveformPlot

VOLTAGE_RANGES = [0.5, 1.0, 2.0, 5.0]
MAX_SAMPLE_RATE = 100_000_000.0


class MainWindow(QMainWindow):
    def __init__(self, use_mock: bool = False):
        super().__init__()
        self.setWindowTitle("NI USB-5133 DAQ")
        self.resize(1200, 700)

        self._scope = None
        self._worker: AcquisitionWorker | None = None
        self._last_waveform: np.ndarray | None = None
        self._last_sample_rate: float = 1_000_000.0
        self._use_mock = use_mock

        self._build_ui()
        self._connect_signals()

    # ------------------------------------------------------------------
    # UI 빌드
    # ------------------------------------------------------------------

    def _build_ui(self):
        central = QWidget()
        self.setCentralWidget(central)
        root = QVBoxLayout(central)

        root.addWidget(self._build_control_panel())

        splitter = QSplitter(Qt.Vertical)
        self._waveform_plot = WaveformPlot()
        self._fft_plot = FFTPlot()
        splitter.addWidget(self._waveform_plot)
        splitter.addWidget(self._fft_plot)
        splitter.setSizes([400, 250])
        root.addWidget(splitter)

        root.addWidget(self._build_action_bar())

        self._status_bar = QStatusBar()
        self.setStatusBar(self._status_bar)
        self._status_bar.showMessage("준비")

    def _build_control_panel(self) -> QGroupBox:
        group = QGroupBox("설정")
        layout = QHBoxLayout(group)

        # 연결/해제
        self._btn_connect = QPushButton("연결")
        self._btn_disconnect = QPushButton("해제")
        self._btn_disconnect.setEnabled(False)
        layout.addWidget(self._btn_connect)
        layout.addWidget(self._btn_disconnect)

        layout.addWidget(QLabel("|"))

        # 채널 선택
        self._chk_ch0 = QCheckBox("CH0")
        self._chk_ch1 = QCheckBox("CH1")
        self._chk_ch0.setChecked(True)
        layout.addWidget(QLabel("채널:"))
        layout.addWidget(self._chk_ch0)
        layout.addWidget(self._chk_ch1)

        layout.addWidget(QLabel("|"))

        # 샘플레이트
        layout.addWidget(QLabel("샘플레이트 (Hz):"))
        self._edit_sample_rate = QLineEdit("1000000")
        self._edit_sample_rate.setFixedWidth(120)
        layout.addWidget(self._edit_sample_rate)

        layout.addWidget(QLabel("|"))

        # 레코드 길이
        layout.addWidget(QLabel("레코드 길이 (samples):"))
        self._edit_record_length = QLineEdit("1000")
        self._edit_record_length.setFixedWidth(80)
        layout.addWidget(self._edit_record_length)

        layout.addWidget(QLabel("|"))

        # 전압 범위
        layout.addWidget(QLabel("전압 범위:"))
        self._combo_voltage = QComboBox()
        for v in VOLTAGE_RANGES:
            self._combo_voltage.addItem(f"±{v} V", v)
        self._combo_voltage.setCurrentIndex(1)  # 기본 ±1 V
        layout.addWidget(self._combo_voltage)

        layout.addStretch()
        return group

    def _build_action_bar(self) -> QWidget:
        widget = QWidget()
        layout = QHBoxLayout(widget)
        layout.setContentsMargins(0, 0, 0, 0)

        self._btn_start = QPushButton("▶ 시작")
        self._btn_stop = QPushButton("■ 정지")
        self._btn_save = QPushButton("CSV 저장")
        self._btn_stop.setEnabled(False)
        self._btn_save.setEnabled(False)

        layout.addWidget(self._btn_start)
        layout.addWidget(self._btn_stop)
        layout.addStretch()
        layout.addWidget(self._btn_save)
        return widget

    # ------------------------------------------------------------------
    # 시그널 연결
    # ------------------------------------------------------------------

    def _connect_signals(self):
        self._btn_connect.clicked.connect(self._on_connect)
        self._btn_disconnect.clicked.connect(self._on_disconnect)
        self._btn_start.clicked.connect(self._on_start)
        self._btn_stop.clicked.connect(self._on_stop)
        self._btn_save.clicked.connect(self._on_save)

    # ------------------------------------------------------------------
    # 핸들러
    # ------------------------------------------------------------------

    def _on_connect(self):
        try:
            if self._use_mock:
                self._scope = MockScope()
            else:
                self._scope = NI5133()
            self._scope.connect()
            self._btn_connect.setEnabled(False)
            self._btn_disconnect.setEnabled(True)
            self._status_bar.showMessage("디바이스 연결됨")
        except Exception as e:
            QMessageBox.critical(self, "연결 오류", str(e))

    def _on_disconnect(self):
        # 주의: worker.wait(3000) — NI5133 fetch 블로킹 시 최대 3초 지연 가능.
        self._on_stop()
        if self._scope:
            self._scope.disconnect()
            self._scope = None
        self._btn_connect.setEnabled(True)
        self._btn_disconnect.setEnabled(False)
        self._status_bar.showMessage("연결 해제됨")

    def _on_start(self):
        if self._scope is None:
            QMessageBox.warning(self, "경고", "먼저 디바이스를 연결하세요.")
            return

        channels = []
        if self._chk_ch0.isChecked():
            channels.append(0)
        if self._chk_ch1.isChecked():
            channels.append(1)
        if not channels:
            QMessageBox.warning(self, "경고", "최소 하나의 채널을 선택하세요.")
            return

        try:
            sample_rate = float(self._edit_sample_rate.text())
        except ValueError:
            QMessageBox.warning(self, "입력 오류", "샘플레이트에 유효한 숫자를 입력하세요.")
            return

        if sample_rate <= 0 or sample_rate > MAX_SAMPLE_RATE:
            QMessageBox.warning(
                self, "입력 오류",
                f"샘플레이트는 1 ~ {MAX_SAMPLE_RATE:,.0f} Hz 범위여야 합니다."
            )
            return

        try:
            record_length = int(self._edit_record_length.text())
        except ValueError:
            QMessageBox.warning(self, "입력 오류", "레코드 길이에 정수를 입력하세요.")
            return

        voltage_range = self._combo_voltage.currentData()

        try:
            self._scope.configure(sample_rate, record_length, channels, voltage_range)
        except Exception as e:
            QMessageBox.critical(self, "설정 오류", str(e))
            return

        self._last_sample_rate = sample_rate
        self._worker = AcquisitionWorker(self._scope)
        self._worker.data_ready.connect(self._on_data_ready)
        self._worker.error_occurred.connect(self._on_error)
        self._worker.start()

        self._btn_start.setEnabled(False)
        self._btn_stop.setEnabled(True)
        self._status_bar.showMessage("수집 중...")

    def _on_stop(self):
        if self._worker and self._worker.isRunning():
            self._worker.stop()
            self._worker.wait(3000)
        self._worker = None
        self._btn_start.setEnabled(True)
        self._btn_stop.setEnabled(False)
        self._status_bar.showMessage("수집 정지")

    def _on_data_ready(self, waveform: np.ndarray):
        self._last_waveform = waveform
        self._waveform_plot.update_waveform(waveform, self._last_sample_rate)
        self._fft_plot.update_fft(waveform, self._last_sample_rate)
        self._btn_save.setEnabled(True)

    def _on_error(self, message: str):
        self._on_stop()
        self._status_bar.showMessage(f"오류: {message}")
        QMessageBox.critical(self, "수집 오류", message)

    def _on_save(self):
        if self._last_waveform is None:
            return
        filename, _ = QFileDialog.getSaveFileName(
            self, "CSV 저장", "", "CSV Files (*.csv)"
        )
        if not filename:
            return
        try:
            save_waveform(self._last_waveform, self._last_sample_rate, filename)
            self._status_bar.showMessage(f"저장 완료: {filename}")
        except Exception as e:
            QMessageBox.critical(self, "저장 오류", str(e))

    def closeEvent(self, event):
        # 주의: NI5133의 fetch()가 블로킹 중일 경우 3초 타임아웃 후 종료.
        # 실제 하드웨어 사용 시 드라이버가 fetch를 블로킹하면 창 닫기가 지연될 수 있음.
        # MockScope에서는 무관 (non-blocking fetch).
        self._on_disconnect()
        super().closeEvent(event)
```

- [ ] **Step 2: Commit**

```bash
git add usb5133_daq/ui/main_window.py
git commit -m "feat: add main window UI"
```

---

### Task 9: 앱 진입점

**Files:**
- Create: `main.py`

- [ ] **Step 1: main.py 작성**

```python
# main.py
import sys
import argparse

from PyQt5.QtWidgets import QApplication

from usb5133_daq.ui.main_window import MainWindow


def main():
    parser = argparse.ArgumentParser(description="NI USB-5133 DAQ Application")
    parser.add_argument(
        "--mock",
        action="store_true",
        help="실제 하드웨어 없이 MockScope로 실행 (개발/테스트용)",
    )
    args = parser.parse_args()

    app = QApplication(sys.argv)
    window = MainWindow(use_mock=args.mock)
    window.show()
    sys.exit(app.exec_())


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 앱 실행 테스트 (mock 모드)**

```bash
python main.py --mock
```

Expected: GUI 창이 열리고, "연결" → "시작" 후 실시간 파형과 FFT가 표시됨

- [ ] **Step 3: 전체 테스트 최종 실행**

```bash
pytest tests/ -v
```

Expected: 모든 테스트 PASS

- [ ] **Step 4: 최종 Commit**

```bash
git add main.py
git commit -m "feat: add app entry point with --mock flag"
```

---

## 완료 기준 체크리스트

- [ ] `pytest tests/ -v` 전체 통과
- [ ] `python main.py --mock` 실행 시 GUI 정상 표시
- [ ] MockScope 모드에서 실시간 파형 + FFT 갱신 확인
- [ ] CSV 저장 버튼으로 파일 생성 확인
- [ ] 실제 NI USB-5133 연결 시 `python main.py`로 정상 동작 확인
